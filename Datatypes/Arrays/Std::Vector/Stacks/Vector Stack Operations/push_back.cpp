#include <vector>
#include <iostream>

int main(){

    //creates a vector that can hold 10 elements
    std::vector<int>nums(10);

    // the push_back() member function puts in a new element at the

    
    
    return 0;
}