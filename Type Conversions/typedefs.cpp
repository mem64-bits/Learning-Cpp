// The following aliases are identical

/*A typedef (which is short for “type definition”) 
is an older way of creating an alias for a type. 
To create a typedef alias,

we use the typedef keyword mainly for 
 backwards compatability with C but 
 the using keyword is prefered as 
 its easily to get typedefs wrong*/
 
 
typedef long Miles;
using Miles = long;